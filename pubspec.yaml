name: flutter_flavor_gen
description: CLI tool to generate Flutter flavors — Dart files, Android build.gradle, and iOS schemes.
version: 0.1.0
# homepage: https://github.com/yourusername/flutter_flavor_gen

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2
  interact: ^2.1.1
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0